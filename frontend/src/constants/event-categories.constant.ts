/**
 * Default event categories used across event filters and creation.
 */
export const DEFAULT_EVENT_CATEGORIES = ['Tech', 'Workshop', 'Networking', 'Design', 'AI & Data'] as const;

/**
 * Default fallback category selection for event forms.
 */
export const DEFAULT_EVENT_CATEGORY = DEFAULT_EVENT_CATEGORIES[0];

