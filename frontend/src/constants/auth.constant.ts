/**
 * Default password constant for frontend demo user login convenience.
 */
export const DEFAULT_DEMO_PASSWORD = 'Password123!';

/**
 * Storage keys for authentication state persistence.
 */
export const AUTH_STORAGE_KEYS = {
  TOKEN: 'token',
  USER: 'user',
} as const;

